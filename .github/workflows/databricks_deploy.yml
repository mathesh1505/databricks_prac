name: Deploy Databricks Asset Bundle

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Install Databricks CLI v2
      - name: Install Databricks CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
          databricks version

      # Configure Databricks authentication (PAT-based)
      - name: Set Databricks credentials
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          mkdir -p ~/.databricks
          cat <<EOF > ~/.databricks/config.yaml
          profiles:
            prod:
              host: ${DATABRICKS_HOST}
              token: ${DATABRICKS_TOKEN}
          EOF

      # Validate the Asset Bundle
      - name: Validate Databricks bundle
        working-directory: asset_bundle_demo
        run: databricks bundle validate --target prod

      # Deploy the Asset Bundle
      - name: Deploy Databricks bundle
        working-directory: asset_bundle_demo
        run: databricks bundle deploy --target prod
