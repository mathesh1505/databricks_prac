# The main pipeline for asset_bundle_demo

resources:
  pipelines:
    asset_bundle_demo_etl:
      name: asset_bundle_demo_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/asset_bundle_demo_etl"

      libraries:
        - glob:
            include: ../src/asset_bundle_demo_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
